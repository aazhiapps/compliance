# ============================================
# ComplianCe - Environment Configuration
# ============================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to version control

# ============================================
# Server Configuration
# ============================================

# Server port - default is 8080
PORT=8080

# Environment mode: development, production, or test
NODE_ENV=development

# ============================================
# Security Configuration
# ============================================

# JWT Secret - REQUIRED (minimum 32 characters for security)
# Generate a secure secret with: openssl rand -base64 32
# ⚠️ NEVER use the demo secret in production!
JWT_SECRET=demo-secret-key-please-change-in-production-min-32-chars

# ============================================
# CORS Configuration
# ============================================

# CORS Origin - controls which domains can access your API
# Development: use "*" to allow all origins
# Production: set to your frontend domain (e.g., https://yourdomain.com)
CORS_ORIGIN=*

# ============================================
# Payment Gateway Configuration
# ============================================

# Razorpay Configuration (Required for payment features)
# Get your keys from: https://dashboard.razorpay.com/app/keys
# For testing, use test keys that start with "rzp_test_"
# For production, use live keys that start with "rzp_live_"
VITE_RAZORPAY_KEY=rzp_test_YOUR_KEY

# ============================================
# Optional Services
# ============================================

# Builder.io CMS Integration (Optional)
# Get your key from: https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__

# Custom ping message for the /api/ping endpoint (Optional)
PING_MESSAGE="ping pong"

# ============================================
# Notes
# ============================================
# - Variables prefixed with VITE_ are exposed to the frontend
# - All other variables are server-side only
# - Required variables: JWT_SECRET, VITE_RAZORPAY_KEY
# - Optional variables: PING_MESSAGE, VITE_PUBLIC_BUILDER_KEY
# - For production, ensure JWT_SECRET is at least 32 characters
# - For production, set NODE_ENV=production
# - For production, set CORS_ORIGIN to your actual frontend domain
